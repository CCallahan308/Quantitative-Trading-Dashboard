"""
This test harness was archived to `legacy/test_backtest.py` during repository cleanup.
The original implementation (archived) lives in `legacy/test_backtest.py`.
To test the maintained dashboard, use `Quant_Dashboard.py`.
"""

import sys

print("This legacy test harness has been archived to legacy/test_backtest.py. Use Quant_Dashboard.py for the dashboard or run the archived test in legacy/ if needed.")
sys.exit(0)
